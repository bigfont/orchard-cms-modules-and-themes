@using Orchard.ContentManagement
@{
    ContentItem contentItem = Model.Content.ContentItem.ContentMenuItemPart.Content;
}

@if (contentItem != null) {    
    @Display.MenuItemLink__Bootstrap(Href: Model.Href, Text: Model.Text, BootstrapAttributes: Model.BootstrapAttributes, BootstrapIcon: Model.BootstrapIcon)
}